/// Defines the closest distance in pixels between the selection and
/// the document edge before auto-scrolling.
///
/// It's equivalent to the selection handle offset at the first line.
const defaultAutoScrollBoundary = 49.0;
